{% extends '::base.html.twig' %}

{% block body -%}
     {% include "HGREntityBundle:Product:menu-top.html.twig" %}
    <form action="{{ path('product_create') }}" method="post" {{ form_enctype(form) }}>
         <fieldset><legend>Product creation</legend>        
            {{ form_widget(form) }}            
            <p>
                <button type="submit">Create</button>
                <a class="btn" href="{{ path('product') }}">Cancel</a>
            </p>
        </fieldset>
    </form>
{% endblock %}
